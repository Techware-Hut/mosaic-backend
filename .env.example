# Core runtime

PORT=3001

NODE_ENV=development

MONGODB_URI=mongodb://localhost:27017/mbh

FRONTEND_URL=http://localhost:3000

# Comma-separated browser origins allowed for credentialed CORS (production).
# The apex domain is the canonical marketplace frontend. Keep app.mosaicbizhub.com
# only as a temporary transition origin until final-domain auth/payment smoke passes.
# CORS_ORIGINS=https://mosaicbizhub.com,https://app.mosaicbizhub.com,https://mosaic-biz-frontend-launch.vercel.app

# Optional rollback flag for generated/sanitized frontend redirects.
# Keep false/unset in normal production so Stripe/OAuth/billing redirects use mosaicbizhub.com.
ALLOW_LEGACY_FRONTEND_ORIGIN=false

JWT_SECRET=replace-with-a-long-random-secret

COOKIE_DOMAIN=

COOKIE_SECURE=false

COOKIE_SAMESITE=lax



# Google OAuth (required at boot — authController throws if missing)

GOOGLE_CLIENT_ID=replace-me

GOOGLE_CLIENT_SECRET=replace-me

API_BASE_URL=http://localhost:3001



# Stripe — use names that match code/README (not legacy STRIPE_WEBHOOK_SECRET names)

STRIPE_SECRET_KEY=sk_test_replace_me

STRIPE_ORDER_WEBHOOK_SECRET=whsec_replace_order_status_webhook

STRIPE_BUSINESS_DRAFT_WEBHOOK_SECRET=whsec_replace_business_draft_webhook

STRIPE_SUBSCRIPTION_WEBHOOK_SECRET=whsec_replace_subscription_webhook

STRIPE_VENDOR_VERIFICATION_WEBHOOK_SECRET=whsec_replace_vendor_verification_webhook

STRIPE_ORDER_POST_PAYMENT_WEBHOOK_SECRET=whsec_replace_order_email_webhook

PLATFORM_FEE_CENTS=100

BILLING_PORTAL_RETURN_URL=http://localhost:3000/billing

CONNECT_RETURN_PATH=/partners/connect/return

CONNECT_REFRESH_PATH=/partners/connect/refresh

# Prefer FRONTEND_URL + CONNECT_RETURN_PATH in production. Use full URL overrides only for an intentional QA/cutover window.
CONNECT_RETURN_URL=http://localhost:3000/partners/connect/return

# Prefer FRONTEND_URL + CONNECT_REFRESH_PATH in production. Use full URL overrides only for an intentional QA/cutover window.
CONNECT_REFRESH_URL=http://localhost:3000/partners/connect/refresh



# AWS S3

AWS_REGION=us-east-1

AWS_ACCESS_KEY_ID=replace-me

AWS_SECRET_ACCESS_KEY=replace-me

AWS_S3_BUCKET=replace-me



# Email and notifications (Gmail SMTP via Nodemailer — use a Google App Password for MAIL_PASSWORD)

MAIL_USER=you@example.com

MAIL_PASSWORD=replace-with-google-app-password

ADMIN_EMAIL=admin@example.com

SUPPORT_EMAIL=support@example.com

APP_NAME=Mosaic Biz Hub

APP_URL=http://localhost:3000



# Optional storage/provider settings

STORAGE_PROVIDER=aws

CLOUDINARY_CLOUD_NAME=replace-me-if-used

CLOUDINARY_API_KEY=replace-me-if-used

CLOUDINARY_API_SECRET=replace-me-if-used



# Google (optional beyond OAuth boot requirements)

GOOGLE_GEOCODING_API_KEY=replace-me-if-used



# PayPal

PAYPAL_CLIENT_ID=replace-me-if-used

PAYPAL_CLIENT_SECRET=replace-me-if-used



# PDF / browser

PUPPETEER_EXECUTABLE_PATH=



# Debug

LISTING_DEBUG=false



# Sentry (optional — set SENTRY_DSN in EB only; never commit real DSN)

# RELEASE_COMMIT_SHA=local-dev-sha

# RELEASE_ENVIRONMENT=development

# DEPLOYMENT_VERSION_LABEL=mosaic-local

# SENTRY_DSN=

# SENTRY_ENVIRONMENT=production

# SENTRY_RELEASE=mosaic-local

# SENTRY_TRACES_SAMPLE_RATE=0

# SENTRY_PROFILES_SAMPLE_RATE=0

# SENTRY_ENABLED=true

# ENABLE_SENTRY_DEBUG_ROUTE=false

